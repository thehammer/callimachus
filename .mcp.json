{
  "mcpServers": {
    "callimachus": {
      "command": "calli",
      "args": ["mcp"],
      "description": "Callimachus index server — queryable entity graph, summaries, contracts, and purposes for this codebase (serves the global production index)"
    }
  }
}
