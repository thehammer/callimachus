{
  "mcpServers": {
    "callimachus": {
      "command": "calli",
      "args": ["--pinakes", "${workspaceFolder}/data/callimachus.pinakes", "mcp"],
      "description": "Callimachus index server — queryable entity graph, summaries, contracts, and purposes for this codebase"
    }
  }
}
