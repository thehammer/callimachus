//! `Edge` now lives in `callimachus-adapter-contract`.
//!
//! Part of the adapter type closure; re-exported here at the historical path so
//! all existing `crate::types::edge::…` references keep resolving.
pub use callimachus_adapter_contract::types::edge::Edge;
